/** @type {import('next').NextConfig} */
const nextConfig = {
    // Configure images in next js
    images: {
        domains: [
            "oaidalleapiprodscus.blob.core.windows.net",
        ]
    }
}

module.exports = nextConfig
